rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny direct client access. Use Cloud Functions as the API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
